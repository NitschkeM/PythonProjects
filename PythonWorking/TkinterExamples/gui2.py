import sys
from tkinter import *

# Button(None, text='sys.exit', command=sys.exit).pack()
# mainloop()


# exitBtn = Button(None, text='sys.exit', command=sys.exit)
# exitBtn.pack()
# exitBtn.mainloop()


# root = Tk()
# Button(None, text='root.quit', command=root.quit).pack(side=LEFT)    # Quit all windows
# # Button(None, text='root.destroy', command=root.destroy).pack()    # Destroy one window
# root.mainloop()


# root = Tk()
# Button(None, text='root.destroy', command=root.destroy).pack(side=LEFT, expand=YES) # Centred Btn
# root.mainloop()


# def packLabel():
#     Label(None, text='Label').pack()

# root = Tk()
# Button(None, text='pack label', command=packLabel).pack(side=LEFT, expand=YES)
# root.mainloop()
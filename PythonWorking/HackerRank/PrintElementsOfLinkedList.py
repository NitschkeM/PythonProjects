def printLinkedList(head):
    while head is not None:
        print(head.data)
        head = head.next
# def printLinkedList(address):
#     while address is not None:
#         print(address.data)
#         address = address.next


# variableName = None
# variableName = 15

# if variableName is not None:
#     print(variableName)